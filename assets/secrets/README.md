# Temporary embedded FCM credentials

Place the temporary Firebase service-account JSON at:

`assets/secrets/firebase-service-account.json`

The JSON file is intentionally ignored by Git. This embedded credential approach
is only for local/prototype use and must be replaced by a trusted sender before
shipping a production build.
